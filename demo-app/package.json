{
  "name": "@monorepo/demo-app",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "dev:production": "PORT=3000 NODE_ENV=production bun run src/index.ts",
    "dev:staging": "PORT=3001 NODE_ENV=staging bun run src/index.ts",
    "dev:development": "PORT=3002 NODE_ENV=development bun run src/index.ts",
    "start": "bun run src/index.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "vet": "tsc && biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/bun": "catalog:"
  },
  "dependencies": {
    "@monorepo/utils": "workspace:*"
  },
  "peerDependencies": {
    "@typescript/native-preview": "catalog:"
  }
}
